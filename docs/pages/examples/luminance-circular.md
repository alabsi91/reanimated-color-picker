# Luminance Circular

The same nesting trick with a luminance ring instead of a hue one, wrapped around a `<Panel3 />` wheel — two circular controls in
one. `thumbScaleAnimationValue={1}` holds the ring's thumb at a fixed size while it is dragged.

<div class="example-container">
  <react-native src="@examples/LuminanceCircular.tsx" bundle></react-native>
</div>

## Code

<div>
  <code-block lang="tsx" title="LuminanceCircular.tsx" collapsible="10">
    <inline-text src="@examples/LuminanceCircular.tsx"></inline-text>
  </code-block>
</div>

<markdown src="@parts/markdown/color-picker-style.md"></markdown>
