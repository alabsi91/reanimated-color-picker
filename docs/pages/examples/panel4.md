# Panel4

A single square covering all three channels at once — saturation and brightness horizontally, hue vertically — so hue needs no
slider of its own. Its color range is limited, so the thumb cannot land on every possible color.

<div class="example-container">
  <react-native src="@examples/Panel4.tsx" bundle></react-native>
</div>

## Code

<div>
  <code-block lang="tsx" title="Panel4.tsx" collapsible="10">
    <inline-text src="@examples/Panel4.tsx"></inline-text>
  </code-block>
</div>
