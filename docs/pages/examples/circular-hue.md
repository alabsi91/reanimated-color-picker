# Circular Hue

A hue ring wrapped around a `<Panel1 />`, so the panel sits inside the slider rather than beside it. Any children of
`<HueCircular />` are laid out in the middle of the ring — the panel here is simply sized to 70% and centered.

<div class="example-container">
  <react-native src="@examples/CircularHue.tsx" bundle></react-native>
</div>

## Code

<div>
  <code-block lang="tsx" title="CircularHue.tsx" collapsible="10">
    <inline-text src="@examples/CircularHue.tsx"></inline-text>
  </code-block>
</div>

<markdown src="@parts/markdown/color-picker-style.md"></markdown>
